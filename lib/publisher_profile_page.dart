import 'package:flutter/material.dart';

class PublisherProfilePage extends StatelessWidget {

  PublisherProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    
    return Scaffold(

      appBar: AppBar(
        title: const Text("Perfil de publicador"),
      ),
    );
  }

}